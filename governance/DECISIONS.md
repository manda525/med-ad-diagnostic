# DECISIONS

意思決定の記録。新しい決定は**上に**追加する。撤回する場合は削除せず、状態を変えて理由を残す。

状態：`採用` / `保留` / `撤回` / `検討中`

このリポジトリは public。決定の背景に非公開情報が含まれる場合は、ここには結論だけを書き、詳細は `.ai-private/` に置く。

---

## D-006　CBD・カンナビノイド関連製品を事業領域に含める

- 日付：2026-08-06
- 状態：**採用**
- 決定者：まさ

規制が最も大きく動く領域という判断から、対象領域に加える。

関係法令は薬機法に閉じず、大麻草の栽培の規制に関する法律／麻薬及び向精神薬取締法、食品衛生法・食品表示法、景表法、輸入手続が絡む。

**施行時期・残留限度値・輸入時の必要書類は未確認。** クライアント業務で用いる前に一次情報での裏取りと確認日の記録が必要。診断ツールへの組み込みは未着手で、着手する場合は別RFC。

---

## D-005　ルール件数を対外訴求に使わない（定義が確定するまで）

- 日付：2026-08-06
- 状態：**採用**

正本・本番アプリ・過去表記で件数の定義が並存しているため、確定するまで具体的件数を対外的な訴求に使わない。内訳は `.ai-private/HANDOFF.md` に記録。

---

## D-004　診断モデルを環境変数で切り替え可能に保ち、当面は Sonnet 5 を使う

- 日付：2026-08-05
- 状態：**採用（暫定）**

既定モデルでは応答が実行時間の上限に達し、診断が成立しない状態だった。`DIAGNOSE_MODEL` で切り替え、当面は応答時間と原価の両面から Sonnet 5 を用いる。

モデル固有のパラメータは対象モデルのときだけ送る（無条件に送ると切り替え時に400になる）。

恒久対応（実行時間上限の見直し、ストリーミング、難判定のルーティング）は後続で扱う。価格・仕様は記憶で断定せず、公式の一次情報を確認して確認日を添える。

---

## D-003　秘密鍵が無い場合は fail closed

- 日付：2026-08-05
- 状態：**採用**

`APP_TOKEN_SECRET` が未設定のとき、既定値へフォールバックして動作を継続しない。Cookieの署名もIPの秘匿も成立しなくなり、利用枠の判定が骨抜きになるため。診断APIは 503 で停止する。

---

## D-002　利用枠は「予約してから使い、失敗したら戻す」

- 日付：2026-08-05
- 状態：**採用**

上限を確認してから成功後に加算する方式は、同時に到着したリクエストが全て上限未満と判定されて通り抜ける。AI呼び出し前に枠を予約し、加算・上限判定・超過時の巻き戻しを Lua スクリプト1本で不可分に実行する。

AI失敗・タイムアウト・応答解析失敗時は予約を戻す。利用者が結果を受け取れなかった回数を失わないため。

---

## D-001　publicリポジトリでは、修正が本番に届く前に差分を公開しない

- 日付：2026-08-05
- 状態：**採用**

セキュリティ修正をGitHubへ先に出すと、修正が利用者に届く前に弱点の所在が公開される。ローカル → プレビュー → 本番の順で反映し、**その後に** push・PR を行う。

PR本文には影響の概要・修正内容・テスト結果・本番反映済みである旨・ロールバック方法・未確認事項のみを書き、攻撃手順や再現コマンドは書かない。
